dependencies:
  pre:
    - sudo apt-get install libicu52
    - curl --output /home/ubuntu/bin/phantomjs-2.0.1-linux-x86_64-dynamic https://s3.amazonaws.com/circle-support-bucket/phantomjs/phantomjs-2.0.1-linux-x86_64-dynamic
    - chmod a+x /home/ubuntu/bin/phantomjs-2.0.1-linux-x86_64-dynamic
    - sudo ln -s --force /home/ubuntu/bin/phantomjs-2.0.1-linux-x86_64-dynamic /usr/local/bin/phantomjs
    - curl -L https://github.com/manastech/crystal/releases/download/0.7.7/crystal-0.7.7-1-linux-x86_64.tar.gz | tar zx
    - sudo ln -s --force /home/ubuntu/abilitysheet/crystal-0.7.7-1/bin/crystal /usr/local/bin/crystal

machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.2.2

test:
  pre:
    - bundle exec rake bower:install
    - bundle exec rake bower:resolve
  override:
    - "git submodule update --init"
    - "mv .submodules/api-abilitysheet/.env.circleci .submodules/api-abilitysheet/.env"
    - "cd .submodules/api-abilitysheet && crystal deps"
    - "cd .submodules/api-abilitysheet && crystal src/app.cr": { background: true }
    - "bundle exec bin/rubocop_parallel": { parallel: true }
    - bundle exec rspec
