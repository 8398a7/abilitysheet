| こんにちは、
| #{@resource.email}
| さん。
| #{"\r\n"}
| ホームページからパスワードを再発行するリクエストを得ました。
| #{"\r\n"}
| 以下のリンクからパスワードを変更することが出来ます。
| #{"\r\n"}
| #{edit_user_password_url(@resource, reset_password_token: @token)}
| #{"\r\n"}
| もしこのリクエストを行った覚えがない場合、このメールは無視して下さい。
| #{"\r\n"}
| パスワードの再発行は上記リンクで操作を行うまで行われません。
| #{"\r\n"}
| ---------------------------------------------------
| #{"\r\n"}
| ※ このメールアドレスには返信ができません。
