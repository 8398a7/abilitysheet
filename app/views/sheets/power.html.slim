h2
  | サンプルデータはiidx.meより1100人程度を利用
table.table
  thead
    th TITLE
    th EASY
    th CLEAR
    th HARD
    th EXH CLEAR
    th FULL COMBO
    th AAA
  tbody
    - @sheets.each do |sheet|
      tr
        td = sheet.title
        td bgcolor=@color[sheet.id] = sprintf("%.2f", sheet.e)
        td bgcolor=@color[sheet.id] = sprintf("%.2f", sheet.c)
        td bgcolor=@color[sheet.id] = sprintf("%.2f", sheet.h)
        td bgcolor=@color[sheet.id] = sprintf("%.2f", sheet.exh)
        td bgcolor=@color[sheet.id] = sprintf("%.2f", sheet.fc)
        td = sprintf("%.2f", sheet.aaa)
javascript:
  (function() {
    $('.table').dataTable({
      aLengthMenu: [#{ @sheets.count }]
    });
  }).call(this);
