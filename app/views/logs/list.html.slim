= link_to '表のクリア推移', sheet_log_path, class: 'btn btn-warning'

h2 更新履歴
table.datatable
  thead
    th 更新日付
    th 更新個数
  tbody
    - @logs.each do |log|
      tr
        td = link_to log, show_log_path(current_user.iidxid, log)
        td = Log.where(created_at: log).count
coffee:
  $('.datatable').dataTable(
    order: [0, 'desc']
  )
