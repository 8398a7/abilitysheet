test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: iidx-app
  credentials: <%= ENV.fetch('GCLOUD_SERVICE_KEY') { Rails.root.join('abilitysheet-service-account.json') } %>
  bucket: abilitysheet
