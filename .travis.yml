language: ruby

rvm:
  - 2.1.5

cache:
  bundler: true

bundler_args: --jobs=4 --retry=3 --without development --deployment

before_script:
  - cp config/database.travis.yml config/database.yml
  - bundle exec rake db:create
  - bundle exec rake db:migrate
